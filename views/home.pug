extends loggedInLayout

block content
    -if (messages.exists)
        .message.exists
            span=messages.exists
    section
        p #{user.email}
        p #{user.firstName}
            
