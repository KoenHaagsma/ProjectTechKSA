extends layout

//- Inlog + registration.
block content
    section
        form(action="/loginUser" method="POST")
            label(for='email') E-mailadres
            input#e-mail(type='e-mail' name='email' placeholder='example@email.com' required='')

            label(for='password') Password (Minimum of 8 characters)
            input#password(type='password' name='password' minlength="8")
            button(type="submit") Login

            button(type="submit") 
                a(href="/register") Don't have an account yet? Register!









